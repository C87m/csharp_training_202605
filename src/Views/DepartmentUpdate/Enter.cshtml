@* クラスの名前空間を指定する(C#と同じ) *@
@using src.Presentations.ViewModels
@* View が受け取るデータ型（ViewModel）を宣言asp-for はこの型のプロパティと結びつける *@
@model DepartmentUpdateViewModel;
@{
    ViewData["Title"] = "更新部署選択";
}
<h2 class="text-center mt-4">@ViewData["Title"]</h2>
<h3 class="text-center mt-4">選択した部署の情報変更を行います。</h3>

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-4 col-sm-6"> 
            <form asp-controller="DepartmentUpdate" asp-action="Edit" method="get">
                @*部署一覧をプルダウンで表示*@
                <select asp-for="Id" asp-items="Model.Departments" class="form-select"></select>
                </select> 
                @* [選択]ボタン *@
                <br/>
                <div class="d-flex justify-content-center">
                    <button type="submit" class="btn btn-primary">選択</button>
                </div>
            </form>
        </div>
    </div>
</div>